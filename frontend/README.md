Frontend scaffold for Wealth Manager

Run the following to install dependencies and start dev server:

```bash
cd frontend
npm install
npm run dev
```

Tailwind is already configured; pages include `/login`, `/register`, and protected `/dashboard`.
